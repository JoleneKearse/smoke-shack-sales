import styled from "styled-components";

const TotalTitle = styled.h2`
  font-size: 1.5em;
  text-align: center;
`;

const Total = () => {
  return (
    <>
      <TotalTitle>Today's Total</TotalTitle>
    </>
  );
};

export default Total;
